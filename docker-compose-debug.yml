services:
  email_finder:
    image: email_finder:latest
    container_name: email_finder
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
